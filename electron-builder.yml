appId: com.nt-converter.app
productName: NT Converter
directories:
  buildResources: build
  output: dist

files:
  - "!**/.vscode/*"
  - "!src/*"
  - "!electron.vite.config.*"
  - "!{.eslintignore,.eslintrc.cjs,.prettierignore,.prettierrc.yaml,dev-app-update.yml,CHANGELOG.md,README.md}"
  - "!{tsconfig.json,tsconfig.node.json,tsconfig.web.json}"

extraResources:
  - from: resources/ffmpeg/${platform}-${arch}/
    to: ffmpeg/
    filter:
      - "**/*"

win:
  target:
    - target: nsis
      arch: [x64]
  icon: build/icon.png
  artifactName: "NT-Converter-${version}-win-${arch}.${ext}"

nsis:
  oneClick: true
  perMachine: false
  deleteAppDataOnUninstall: true

mac:
  target:
    - target: dmg
      arch: [x64, arm64]
    - target: zip
      arch: [x64, arm64]
  icon: build/icon.png
  artifactName: "NT-Converter-${version}-mac-${arch}.${ext}"
  hardenedRuntime: true
  gatekeeperAssess: false
  entitlements: build/entitlements.mac.plist
  entitlementsInherit: build/entitlements.mac.plist

publish:
  provider: github
  owner: CookseyNiceTouch
  repo: nt_converter
  releaseType: release
